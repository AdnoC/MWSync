/**
 * This will be a class that stores usernames and passwords. At the moment, this
 * is a shell that I copy at each dev enviromnent and hard-code my personal username
 * and passwords, so that it is secure and convenient.
 */
public class Config {
  public static final String MWUSERNAME = "";
  public static final String MWPASSWORD = MD5Gen.encryptPass(USERNAME, "");
  public static final String MAL_USERNAME = "";
  public static final String MAL_PASSWORD = "";
}
